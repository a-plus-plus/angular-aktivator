describe 'Answering to a survey', ->
	# beforeEach (
	# 	inject( (_$httpBackend_) ->
	# 		$httpBackend = _$httpBackend_		
	# 		$httpBackend.expectGet('localhost:3000').respond({'title':'asd'})
	# 		browser().navigateTo('/#/answer/1')
	# 	)
	# )
	# it 'shows the title of the site', ->
		
	# 	expect(element('h1').text()).toBe('Answer to a survey')
	# 	sleep()

	# it 
