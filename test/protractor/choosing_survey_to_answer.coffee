'use strict'
By = protractor.By


describe 'Choosing a survey from survey listing', ->

	# PENDING tests for survey listing!

	# it 'has the proper title', ->

	# it 'shows status of every published and finished survey', ->

	# it 'shows the titles to published surveys', ->

	# it 'shows the titles to finished surveys', ->

	# it 'does not show the titles of unpublished surveys', ->

	# it 'allows answering to a published survey', ->

	# it 'will not allow answering to unpublished surveys', ->

	# it 'will not allow answering to finished surveys', ->

	# it 'allows editing a survey only if user is logged in', ->

	# it 'allows viewing the results of a survey only if user is logged in', ->